---
---

<section class="section1" data-parallax>
    <div class="hero-bg"></div>
    <div class="sand-overlay"></div>
    <div class="logo mx-auto max-w-[80%] md:max-w-[40%] mb-50">
      <img 
          src="/assets/LOGOTIPO_COLOR_2.png" 
          alt="Logo Hotel Las Aldas"
          width="800"
          height="400"
          fetchpriority="high"
          decoding="async"
      />
    </div>
</section>

<script>
  const section = document.querySelector('[data-parallax]') as HTMLElement | null;
  const heroBg = document.querySelector('.hero-bg') as HTMLElement | null;
  const logo = document.querySelector('.logo') as HTMLElement | null;

  let ticking = false;

  function updateParallax() {
    const scrolled = window.pageYOffset;
    const sectionRect = section?.getBoundingClientRect();

    if (sectionRect && sectionRect.top < window.innerHeight && sectionRect.bottom > 0) {
      if (heroBg) {
        heroBg.style.transform = `translateY(${scrolled * 0.3}px)`;
      }
      if (logo) {
        // translate3d en lugar de translateY → mueve solo en GPU, sin recalcular layout
        logo.style.transform = `translate3d(0, ${scrolled * 1}px, 0)`;
      }
    }

    ticking = false;
  }

  window.addEventListener('scroll', () => {
    if (!ticking) {
      window.requestAnimationFrame(updateParallax);
      ticking = true;
    }
  }, { passive: true }); // passive evita que el navegador espere al JS antes de hacer scroll
</script>